// Use slice to extract parts of the string, end not included
// The starting index is inclusive, the ending index - 4, is exclusive (not included)
let text = "JavaScript";
console.log(text.slice(0,4)); // Java

console.log(text.slice(5, -1));