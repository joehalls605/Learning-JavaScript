// Create a Greeting Message:
// TODO: Create a template literal named `greeting` that includes the name variable and a friendly greeting. 
// Log the result.

const name = "Joe"
const templateLiteral = `Hello there ${name}`;
console.log(greeting);

// Generate a URL:
// TODO: Construct a URL using template literals by combining a base URL and a resource path. Log the complete URL.

const baseURL = "www.google.com"
const resoucePath = "/home"
const templateLiteral2 = `${url}${resoucePath}`;
console.log(templateLiteral2);

// Display Mathematical Equation:
// TODO: Use template literals to display a mathematical equation, including two variables and the result. Log the equation.

const numOne = 1;
const numTwo = 2;
const result = `${numOne} + ${numTwo} = ${numOne + numTwo}`;
