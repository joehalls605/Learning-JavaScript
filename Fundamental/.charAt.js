/*
The charAt() method in JavaScript is used to get the character at a specific index in a string.
charAt(index) returns the character at the specified index in a string. The index starts at 0 for the first character.
*/

let text = "Hello";
let character = text.charAt(2);
console.log(character); // Outputs: "l"