/*
he slice() method in JavaScript is used to extract a portion of a string or array.

slice(startIndex, endIndex) extracts a section of a string from the startIndex up to, but not including, the endIndex. If endIndex is omitted, it will slice to the end of the string.
*/

let text = "Hello, world!";
let slicedText = text.slice(2,5);