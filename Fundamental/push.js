// Initialise an array with some fruits
let fruits = ['Apple', 'Banana', 'Cherry'];

// Use the push method to add a new fruit
fruits.push('Orange');

// Log the updated array to the console
console.log(fruits);
