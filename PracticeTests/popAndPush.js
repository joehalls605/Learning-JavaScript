// You are given an array of numbers. Use push() to add the number 25 to the end of the array.

let numbers = [5, 10, 15];
numbers.push(25);

// You have an array of strings. Use push() to add two strings, "orange" and "grape", at the end of the array.

let fruits = ["apple", "banana", "cherry"];
fruits.push("orange", "grape");

// You are given an array of mixed values. Use push() to add the value true and "hello" to the end of the array.

let mixedArray = [1, "test", false];
mixedArray.push(true, "hello");

// You are given an array of numbers. Use pop() to remove the last element and log the resulting array.

let numbers = [5, 10, 15, 20];
numbers.pop();
console.log(numbers);

// You have an array of fruit names. Use pop() to remove the last fruit from the array and log the removed fruit.

let fruits = ["apple", "banana", "cherry", "date"];
removedFruit = fruits.pop();
console.log(removedFruit);

// You are given an array of colors. Use pop() to remove the last element and log both the removed color and the updated array.

let colours = [];
removedLastColour = colours.pop();
console.log(removedLastColour);

